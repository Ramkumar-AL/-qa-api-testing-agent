# Optional - without these, /api/generate and AI triage fall back to offline
# rule-based output. Add the same variables in Vercel's Project Settings ->
# Environment Variables when you deploy (see README).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
